<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jht.mapper.Board3Mapper">
	<insert id="write">
		insert into exBoard(title, content, writer, redate)
        values (#{title},#{content},#{writer},#{redate})
	</insert>

	
</mapper>